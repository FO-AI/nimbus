# UX pass screenshots

Before/after pairs for the navigation and visual-language changes, captured at
1440×950 against the local mock stack (`AI_PROVIDER=mock`, `AUTH_MODE=disabled`).
The local-development auth banner is removed from the frames because it never
appears in a deployed environment.

The projects table shows **fictional sample rows seeded into the local
development database** so the stage pills, source badges, stale-date warning,
and department filter are all visible in one frame. They are not real projects.

`before-*` is `main` at the time of the change; `after-*` is this branch.
